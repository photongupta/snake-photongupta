const assert = require('assert');

describe('test', function() {
  it('test', function() {
    let actual = 5;
    let expected = 5;
    assert.strictEqual(actual, expected);
  });
});
describe('just for test', function() {
  it('just for test', function() {
    let actual = 5;
    let expected = 5;
    assert.strictEqual(actual, expected);
  });
});
